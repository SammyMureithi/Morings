/**
 * Determines whether a string begins with the characters of the specified 
 * string returning true or false as appropriate
 */
const str='Saturday night plans';
console.log(str.startsWith('Sat'))
let regExp=/satu/i;

console.log(str.startsWith('Satu/i'))